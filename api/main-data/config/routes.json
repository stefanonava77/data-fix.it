{
  "routes": [
    {
      "method": "GET",
      "path": "/main-data",
      "handler": "main-data.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/main-data/count",
      "handler": "main-data.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/main-data/:id",
      "handler": "main-data.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/main-data",
      "handler": "main-data.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/main-data/:id",
      "handler": "main-data.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/main-data/:id",
      "handler": "main-data.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
